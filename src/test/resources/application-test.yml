server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

integration:
  hmpps-auth:
    url: "http://localhost:8090/auth"
  tc-auth:
    url: "http://localhost:8090/auth"
  document-management:
    url: "http://localhost:9000"
  manage-users:
    url: "http://localhost:8095"
  template-configuration:
    url: "http://localhost:7979"

spring:
  jpa:
    show-sql: true
    properties:
      hibernate.generate_statistics: true
  datasource:
    hikari:
      data-source-properties:
        sslmode: 'disable'

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

client:
  id: document-generation
  secret: document-generation

tc.client:
  id: template-config
  secret: template-config

db:
  server: "localhost:5432"
  name: "document"
  user: "generate"
  pass: "t3mp14t3s"

service:
  allow-pulling-templates: true
  auto-pull-templates: false
  include-templates:
    - TEST_TEMPLATE_1
